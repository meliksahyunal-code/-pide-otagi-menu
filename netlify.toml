[build]
  publish = "."
  command = "# No build command needed for raw HTML/JS"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Cache-Control = "public, max-age=0, must-revalidate"
